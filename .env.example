# Database
# For local development, you can use Docker:
# docker run --name payload-postgres -p 5432:5432 -e POSTGRES_PASSWORD=password -e POSTGRES_DB=payload -d postgres:15
DATABASE_URI=postgresql://postgres:password@localhost:5432/payload

# Payload Secret - Generate with: openssl rand -hex 32
PAYLOAD_SECRET=your-secret-key-here-min-32-chars-long

# Next.js
NEXT_PUBLIC_SERVER_URL=http://localhost:3001

# Optional: For production on Vercel
# DATABASE_URI=postgresql://user:password@host:5432/database?sslmode=require
# PAYLOAD_SECRET=your-production-secret-key
